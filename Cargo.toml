[package]
name = "cuda-tuple-space"
version = "0.1.0"
edition = "2021"
description = "Tuple space — generative communication via pattern-matching tuples, Linda coordination model for agent fleets"

[dependencies]
serde = { version = "1", features = ["derive"] }
